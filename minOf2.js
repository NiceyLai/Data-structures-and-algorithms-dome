// let minOf2 = function () {
//   if (number[0] > number[1]) {
//     return number[1];
//   } else {
//     return number[0];
//   }
// };

// 第一次优化
let minOf2 = (numbers) => (number[0] > number[1] ? number[1] : number[0]);

// 第二次优化
